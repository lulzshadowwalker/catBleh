part of './lulz_imports.dart';

class LulzConst {
  static final double windowWidth = windowSize.width;
  static final double windowHeight = windowSize.height;
  static const Size windowSize = Size(1153, 648);
  static const Size designSize = Size(1920, 1080);
}
